"""analytics_platform.contracts subpackage skeleton.

This subpackage will host contract-first shared types and protocols as they
are introduced by future tasks. At this stage it is intentionally empty: it
exists only to make the subpackage importable.

No future contracts are exported here. Contract modules must be added as
separate task-scoped files, and only exported from this package once they
actually exist and are stable. This subpackage must not import any heavy
implementation libraries.
"""

from __future__ import annotations

__all__: list[str] = []